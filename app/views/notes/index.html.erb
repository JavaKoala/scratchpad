<div class="grid grid-cols-1 gap-4">
  <div>
    <%= @note.content %>
  </div>

  <div>
    <%= form_with model: @note do |form| %>
      <%= form.rich_textarea :content %>
      <%= form.submit 'Save' %>
    <% end %>
  </div>
</div>
